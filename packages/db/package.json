{
  "name": "@newsie/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply news-agg-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply news-agg-db --remote"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "drizzle-orm": "1.0.0-rc.4"
  },
  "devDependencies": {
    "drizzle-kit": "1.0.0-rc.4",
    "tsx": "^4.23.12",
    "wrangler": "^4.123.0"
  }
}
